class PlayerModel {
    constructor(id, name, ready, points, answers) {
        this.id = id
        this.name = name
        this.ready = ready
        this.points = points
        this.answers = answers
    }
}

export default PlayerModel
